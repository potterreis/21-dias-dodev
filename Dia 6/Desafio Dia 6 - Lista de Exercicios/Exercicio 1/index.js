/* Faça um algoritmo que receba um valor, inteiro e positivo, e usando a estrutura FOR, mostre no console uma contagem de 0 ate o número recebido.*/

let valor = Number(prompt('Insira um numero da sua escolha'))

for(let contador = 0; contador <= valor; contador++){
    console.log(contador)
}